export default function CustomerBookingDetailPage({ params }: { params: { id: string } }) {
  // TODO: booking detail, payment button, review form (if COMPLETED)
  return <div>Booking Detail: {params.id}</div>;
}
